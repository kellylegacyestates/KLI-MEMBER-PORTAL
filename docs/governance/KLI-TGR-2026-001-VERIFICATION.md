# KLI-TGR-2026-001 Verification Register

## Register control

| Field | Value |
| --- | --- |
| Governing document | [KLI-TGR-2026-001](./KLI-TGR-2026-001.md) |
| Verification baseline | `4008e3ff0f2018bd6ced9bffb3861530bb7f9f67` |
| Verification date | August 16, 2026 |
| Evidence boundary | Repository contents plus GitHub API branch/workflow metadata |
| Allowed statuses | VERIFIED; PARTIALLY VERIFIED; NOT VERIFIED; NOT APPLICABLE |

`VERIFIED` means the repository or authoritative API establishes the scoped assertion. It does not substitute for external Firebase, Google Cloud, DNS, or runtime evidence.

## Assertions

| ID | Assertion | Evidence | Status | Repository Reference | Required Action |
| --- | --- | --- | --- | --- | --- |
| IA-01 | The application uses the recorded Next.js, React, and TypeScript versions. | Dependency manifest records Next.js 16.2.11, React 19.2.4, and TypeScript 5; strict mode is enabled. | VERIFIED | `package.json` (`dependencies`, `devDependencies`); `tsconfig.json` (`compilerOptions.strict`) | Reverify after dependency changes. |
| IA-02 | The App Router and recorded build configuration are implemented. | Pages and route handlers are under `src/app`; build uses `next build`; Next config is minimal. | VERIFIED | `src/app/`; `package.json` (`scripts.build`); `next.config.ts`; `tsconfig.json` | Document any future custom build or routing controls. |
| IA-03 | Firebase Authentication email/password sign-in and registration are implemented and operationally enabled. | Browser code implements email/password sign-in and account creation. Provider enablement cannot be established from source. | PARTIALLY VERIFIED | `src/components/auth/LoginForm.tsx` (`handleSubmit`); `src/lib/firebase/registerUser.ts` (`registerUser`) | **REQUIRES EXTERNAL CONSOLE VERIFICATION:** confirm provider enablement, password policy, authorized domains, and abuse controls. |
| IA-04 | A non-enumerating Firebase password-reset flow is implemented and operationally enabled. | Code calls Firebase password reset and returns a generic success message. Email delivery and template configuration are external. | PARTIALLY VERIFIED | `src/components/auth/ForgotPasswordForm.tsx` (`handleSubmit`) | **REQUIRES EXTERNAL CONSOLE VERIFICATION:** test delivery, authorized domains, and templates. |
| IA-05 | Firestore client and trusted server initialization are implemented. | Client uses `getFirestore`; server uses Firebase Admin with ADC or complete server-only credentials. | VERIFIED | `src/lib/firebase/client.ts`; `src/lib/firebase/admin.ts` | Verify runtime identity and least-privilege IAM externally. |
| IA-06 | Firebase App Hosting configuration exists and the production backend is deployed. | Backend and runtime configuration exist; source control does not prove an active deployment. | PARTIALLY VERIFIED | `firebase.json` (`apphosting`); `.firebaserc`; `apphosting.yaml` | **REQUIRES EXTERNAL CONSOLE VERIFICATION:** deployment status, backend revision, domain mapping, runtime identity, and trigger. |
| IA-07 | Trusted Firebase Admin session-cookie creation, verification, and revocation are implemented. | Session route validates origin, rate limit, ID token, verified email, and recent sign-in before cookie creation; server verifies cookies with revocation checking; revoke routes exist. | VERIFIED | `src/app/api/auth/session/route.ts` (`POST`); `src/lib/auth/server.ts` (`verifySessionCookie`); `src/lib/auth/cookies.ts`; `src/app/api/auth/logout-all/route.ts` | Perform runtime integration testing in the deployed environment. |
| IA-08 | A Firestore-backed member profile exists at `users/{uid}` with the recorded fields. | Client and server profile code read the `users` collection and define the same UID-keyed record shape. | VERIFIED | `src/lib/firebase/userProfile.ts` (`UserProfile`, `USERS_COLLECTION`); `src/lib/auth/server.ts` (`getAuthenticatedMemberProfile`); `firestore.rules` (`match /users/{uid}`) | Maintain schema changes in this record and rules. |
| IA-09 | Registration maps the Firebase UID to the profile and applies least-privileged defaults. | Auth UID is passed to profile creation; role is member, account active, membership pending; failed profile writes attempt Auth rollback. | VERIFIED | `src/lib/firebase/registerUser.ts` (`registerUser`); `src/lib/firebase/userProfile.ts` (`createUserProfile`) | Add end-to-end onboarding tests and complete verification-email delivery. |
| IA-10 | Roles, membership states, account states, tiers, and other access attributes are accurately represented. | Four roles, three account states, and five membership states exist. No tier or generalized entitlement field exists. | VERIFIED | `src/lib/firebase/userProfile.ts`; `src/lib/firebase/userProfileValidators.ts` | Do not assert subscription tiers or entitlements until a controlled schema exists. |
| IA-11 | Protected routes distinguish unresolved, authorized, and denied states without exposing protected content. | Client state distinguishes loading; server authorization returns explicit outcomes; inspected pages nest content inside server guards. No guarded-content flash path was identified. | VERIFIED | `src/components/auth/AuthProvider.tsx`; `src/lib/auth/server.ts`; `src/components/auth/ServerRouteGuards.tsx`; `src/lib/auth/routes.ts`; protected `src/app/**/page.tsx` files; `src/proxy.ts` | Add integration tests for all protected-route classes and maintain server guards. |
| IA-12 | Firestore Security Rules exist, are configured, and deny unmatched access by default. | Rules source exists, is referenced by Firebase config, and ends in recursive deny. Deployment is not proven. | PARTIALLY VERIFIED | `firestore.rules`; `firebase.json` (`firestore.rules`) | **REQUIRES EXTERNAL CONSOLE VERIFICATION:** compare deployed ruleset and release time with this source. |
| IA-13 | Rules enforce UID ownership and member profile access controls. | Self-read/create/update checks use authenticated UID; deletes are denied; update diff limits member-editable fields. | VERIFIED | `firestore.rules` (`match /users/{uid}`) | Add emulator-based rules tests. |
| IA-14 | Rules and trusted server paths protect privilege fields, administrative access, and authorization writes. | Client updates cannot change protected fields; admin checks use active Firestore role; trusted authorization updates validate input and atomically audit changes. Create validation is not comprehensive for types, sizes, or extra keys. | PARTIALLY VERIFIED | `firestore.rules` (`isAdmin`, `users`, `auditEvents`, `publications`); `src/app/api/admin/users/authorization/route.ts` | Add create/update schema validation and automated rules tests; verify deployed rules externally. |
| IA-15 | CI performs deterministic installation, build validation, and linting. | Main-targeted workflow uses frozen pnpm install, build, and lint on Node.js 22. | VERIFIED | `.github/workflows/ci.yml`; `package.json`; `pnpm-lock.yaml` | Keep action and runtime versions maintained. |
| IA-16 | CI testing, deployment automation, branch assumptions, and branch protection are accurately represented. | CI targets `main`; tests and deployment are absent from the committed workflow. GitHub API reported `main` protected on the verification date, but detailed policy was not inspected. | PARTIALLY VERIFIED | `.github/workflows/ci.yml`; `package.json` (`scripts.test`); GitHub Branches API observation dated August 16, 2026 | Add `pnpm test`; define deployment control; externally review required checks, approvals, bypasses, and environment protections. |
| IA-17 | Environment-file exclusions and Firebase client configuration are accurately represented. | `.env*` is ignored except the example; client reads browser-visible environment variables; App Hosting declares Web SDK configuration. | VERIFIED | `.gitignore`; `.env.example`; `src/lib/firebase/client.ts`; `apphosting.yaml` | Establish an environment ownership and promotion register. |
| IA-18 | Privileged credentials are absent from tracked source and production references are identified without disclosure. | Inspection found placeholder server-secret examples and browser-visible Firebase client configuration, but no tracked private key or service-account credential. Production project references are present. | VERIFIED | `.env.example`; `.gitignore`; `apphosting.yaml`; `.firebaserc`; `src/lib/firebase/admin.ts` | Continue automated secret scanning; verify runtime secret bindings and IAM externally. |
| IA-19 | Implemented administrative, content-administrative, system-administrative, claims-based, and Firestore roles are accurately represented. | Firestore roles are member, instructor, executive, and admin. One general admin role exists; no distinct content/system admin or custom-claim role authority exists. | VERIFIED | `src/lib/firebase/userProfile.ts` (`UserRole`); `src/lib/auth/server.ts` (`requireExecutive`, `requireAdmin`); `scripts/provision-admins.mjs` | Define role separation only through approved institutional policy and threat analysis. |
| IA-20 | Application audit logging exists with the recorded scope and protections. | Server-only records cover all-device revocation, admin revocation, and authorization changes; browser access is denied. UI and event coverage are incomplete. | PARTIALLY VERIFIED | `src/lib/auth/audit.ts`; `src/app/api/auth/logout-all/route.ts`; `src/app/api/admin/users/revoke-sessions/route.ts`; `src/app/api/admin/users/authorization/route.ts`; `firestore.rules`; `src/app/admin/audit/page.tsx` | Define audit scope, viewer/access process, integrity review, retention, export, and alerting. |
| IA-21 | Operational monitoring and alerting are established. | Only selected console diagnostics are implemented; no provider, alert policy, escalation, or service objective is configured in source. | NOT VERIFIED | `src/lib/auth/loginDiagnostics.ts`; `src/app/api/auth/session/route.ts`; `src/app/api/admin/users/authorization/route.ts` | **REQUIRES EXTERNAL CONSOLE VERIFICATION:** inventory any external logging/monitoring; otherwise implement approved monitoring and alerts. |
| IA-22 | Backup, recovery, and data-retention controls are established. | No applicable Firestore backup, recovery, retention, or TTL configuration is present. Root backup statements concern legacy Supabase plans. | NOT VERIFIED | `DATABASE.md` (`Data Backup & Recovery`, historical); repository-wide configuration inspection | **REQUIRES EXTERNAL CONSOLE VERIFICATION:** inspect Firestore backup schedules and retention; approve and test recovery and retention procedures. |
| IA-23 | External Firebase and hosting operational assertions have been verified in the appropriate consoles. | This review used repository and GitHub evidence only. No Firebase or Google Cloud console evidence was available. | NOT VERIFIED | Evidence boundary recorded above | **REQUIRES EXTERNAL CONSOLE VERIFICATION:** Authentication, Firestore, rules releases, App Hosting, IAM, Secret Manager, logging, alerting, backup, and domains. |
